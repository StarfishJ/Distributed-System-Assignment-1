# 若 8080 被占用，可改为 8081 或启动时加 --server.port=8081
server.port=8080

# WebFlux/Netty 线程池配置 - 增加线程数以处理高并发 WebSocket
# 默认是 CPU 核心数，对于高并发 I/O 密集型任务需要更多线程
spring.threads.virtual.enabled=false
# Netty event loop threads (默认是 CPU 核心数 * 2)
# 通过系统属性设置：-Dreactor.netty.ioWorkerCount=32
